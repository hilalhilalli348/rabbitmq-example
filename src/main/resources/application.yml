spring:
  application:
    name: rabbitmq-example
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672

properties:
  rabbitmq:
    exchange-name: user.events.fanout
    mobile-queue-name: mobile.notification.user-created
    desktop-queue-name: desktop.notification.user-created

server:
  port: 8080
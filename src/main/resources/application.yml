spring:
  application:
    name: rabbitmq-example
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672

properties:
  rabbitmq:
    exchange-name: user.events.consistent
    first-queue-name: first.notification.user-created
    second-queue-name: second.notification.user-created

server:
  port: 8080